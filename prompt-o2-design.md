我要给 o2-design 组件库生成 skill 供智能体使用；
source/src/ 中是源码，example/doc/ 中是现有的文档（但是文档已经很久没有更新了所以会出现错漏，因此需要搭配源码确认功能），O2 Design入门.md 这个文档是去年编写的培训文档（其中“辅助工具”的部分不用看，那个工具现在不用）；
需要根据这些信息生成 skill，其中需要包括常见的组件（各种表单组件、列表组件、列表的列组件等）、常见页面搭建（列表页、详情页、详情页有多个表单如何处理、详情页的 tabs 如何处理）、常见工具（$$lov、全局配置等）、常见的坑（响应丢失、性能优化等），不要管 CMS 相关组件；
整体结构可以参考 packages/link-ui/ 的处理方式，对于比较小的功能点/组件，用单个 md 其中记录了简短的示例用法、各类参数、常见问题、特殊使用场景等，目前处理了一些，但是只有 o2-button.md 是正确的处理（简短的示例+参数+插槽+常见问题/特殊使用场景）；

这个 skill 中还存在其他文档，那些先不用管，这个代码仓库中存在其他组件库的 skill，那些也不要管

严格限制：由于 skill 将会分发给其他用户，因此引用的其他文档必须严格限制在 skill 内部或有明确 URL 的在线文档

----- 上面的提示词用不到了, 改用下面的提示词 -----

我要给 o2-design 组件库生成 skill 供智能体使用；
source/src/ 中是源码，example/doc/ 中是现有的文档（但是文档已经很久没有更新了所以会出现错漏，因此需要搭配源码确认功能），O2 Design入门.md 这个文档是去年编写的培训文档（其中“辅助工具”的部分不用看，那个工具现在不用）；
组件文档可以参考 packages\o2-design\skills\o2-design-overview\references\o2-button.md 的处理方式，对于比较小的功能点/组件，用单个 md 记录（简短的示例+参数+插槽+常见问题/特殊使用场景）；

这个 skill 中还存在其他文档，那些先不用管，这个代码仓库中存在其他组件库的 skill，那些也不要管

严格限制：由于 skill 将会分发给其他用户，因此引用的其他文档必须严格限制在 skill 内部或有明确 URL 的在线文档

----- 这个是扫描并修复问题的提示词 -----

扫描整个 packages/o2-design/skills/ 目录, 检查这些问题：
- 索引覆盖检查: 检查是否有文档脱离索引(即没有在 SKILL.md 中列出, 也没有被其他文档引用), 如果有则提示补充索引; 检查索引路径是否正确, 如果有错误则提示修正路径
- 结构检查: 检查文档结构是否合理(如参数表格是否规范, 是否包含示例代码等), 如果有不合理的地方则提示优化结构
- 术语检查: 检查文档内容是否使用统一的术语(如“值集视图”是否统一叫这个名字), 如果有不统一的地方则提示统一术语
- 内容检查: 文档内容是否清晰, 是否存在于实际工作无关的信息, 比如 `ChangeLog 中无相关信息`(没有就不写) `XX内容太多所以不写在这里`(需要说明写在那里而不是说不在这里) 这种无效表述, 如果有则提示删除无关内容并补充实际工作相关内容
- 版本支持检查: 检查文档中的版本支持信息是否准确, 是否有遗漏, 如果有则提示补充准确的版本支持信息

----- 这个是生成 O2Column 相关文档的提示词 -----

接下来开始处理 O2Column 相关文档，这块内容又多又复杂，所以 references 目录下添加一个 column 目录，将其中部分内容的详解放进去（包括 O2Column 的变体组件），去掉 o2-column-family.md 将其内容合并进 o2-column.md，整体结构是：
```
references - o2-column.md
references - column - o2-column-scope-slots.md
references - column - o2-column-input.md
references - column - o2-column-XXX.md
```
其中公共属性/插槽/特殊写法等放在 o2-column.md 中，作用域插槽的使用详解放在 o2-column-scope-slots.md 中，其他变体组件如 O2ColumnInput 的使用说明放在对应的 md 中，最后在 SKILL.md 中补充索引并说明关系(O2Column变体太多, 因此不在 SKILL.md 中一一列出, 但在 o2-column.md 中会说明关系并链接到对应文档)；
source/src/ 中是源码，example/doc/ 中是现有的文档（但是文档已经很久没有更新了所以会出现错漏，因此需要搭配源码确认功能），O2 Design入门.md 这个文档是去年编写的培训文档（其中“辅助工具”的部分不用看，那个工具现在不用）
